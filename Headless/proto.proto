syntax = "proto3";

service GymService {
  rpc Step (Action) returns (StepResponse);
  rpc Reset(Empty) returns (Empty);
}

message Action {
  repeated float action1 = 1;
  repeated float action2 = 2;
}

message Empty {
}

message StepResponse {
  repeated float observation = 1;
  bool done = 2;
}